# Storyweaver 개선사항 누적 관리 문서

이 문서는 개발자 / 디자이너 / MD(콘텐츠 기획) 세 관점에서 발견한 개선사항을 **세션마다 누적**해서 관리합니다.

## 관리 규칙
- **날짜별로 별도 표를 유지합니다.** 새 세션에서 발견한 항목은 그 세션 날짜의 새 표에 등록하며, 한 표는 항목 10개를 채웁니다.
- 각 날짜 표는 **중요도(★) 내림차순**으로 정렬합니다. ID는 최초 등록 순서를 그대로 유지합니다(정렬은 표시 순서만 바꿀 뿐 ID는 바꾸지 않음).
- 항목이 처리되면 **상태만 갱신**합니다 (`제안됨` → `진행중` → `완료`/`보류`). 완료된 항목도 등록된 날짜의 표에 그대로 남아 **과거 기록**이 됩니다 — 이후 날짜의 표로 다시 옮기거나 복제하지 않습니다. 즉 "지금 처리해야 할 것"을 보려면 가장 최근 날짜 표를 보면 되고, 예전 표는 그 시점의 스냅샷으로 고정됩니다.
- 중요도는 ★☆☆☆☆(낮음) ~ ★★★★★(매우 높음) 5단계입니다.
- 각 관점 맨 아래 "🔥 최우선 항목"은 **전체 날짜를 통틀어** 중요도 ★★★★★인 항목만 모은 요약이며, 상태가 바뀔 때 함께 갱신합니다.

**최종 업데이트: 2026-08-06 (사용자가 지목한 개선사항 실행 세션 — 전체 재감사가 아니라 특정 항목 처리 위주라 오늘 표는 10건을 채우지 않고 실제로 다룬 항목만 등록)**

---

## 🎯 지금 처리하면 좋은 상위 10 (완료 제외, 2026-08-06 기준 스냅샷)

날짜별 표 전체를 훑어 **완료/보류가 아닌 항목만** 중요도(★) 내림차순으로 관점별 상위 10개 뽑은 요약입니다. 이 섹션 자체는 소스 오브 트루스가 아니라 아래 날짜별 표에서 파생된 스냅샷이므로, 상태가 바뀌면 원본 항목(해당 날짜 표)만 갱신하면 됩니다 — 이 표는 다음에 다시 뽑을 때 갱신.

### 👨‍💻 개발자
| ID | 개선사항 | 중요도 | 상태 | 메모 |
|---|---|---|---|---|
| DEV-21 | 클라우드 PIN 무단 접근 방지 장치 없음 | ★★★★ | 제안됨 | 6자리 PIN 순차 스캔으로 남의 백업을 읽거나 덮어쓸 수 있음 — 요청 빈도 제한 필요 |
| DEV-05 | IndexedDB 오류 처리 부재 | ★★★★ | 제안됨 | 저장 실패해도 사용자에게 조용히 실패, 피드백 전무 |
| DEV-06 | base64 이미지 인라인 저장으로 DB 비대화 | ★★★★ | 제안됨 | 이미지 리사이즈/재인코딩 없이 원본 그대로 저장 |
| DEV-08 | 데이터 가져오기 시 검증 없음 | ★★★★ | 제안됨 | 손상된 백업 JSON이 앱 상태를 깨뜨릴 수 있음 |
| DEV-14 | CI 파이프라인 부재 | ★★★★ | 제안됨 | 테스트가 77건까지 늘었는데 push/PR 시 자동 실행 안 됨 |
| DEV-03 | 전역 스크립트 구조 확장성 한계 | ★★★★ | 제안됨 | Vite 도입 검토했으나 이번엔 보류 결정(리스크 큼) |
| DEV-31 | UI 로직 테스트 커버리지 확대 | ★★★ | 진행중 | timer/onboarding은 커버됨, manuscript.js·richEditor.js는 아직 |
| DEV-11 | 비공식 서드파티 API 의존 리스크 | ★★★ | 제안됨 | 맞춤법·사전 검색이 공식 계약 없는 프록시에 의존 |
| DEV-25 | 클라우드 동기화 네트워크 실패 시 재시도 없음 | ★★★ | 제안됨 | 실패하면 사용자가 수동으로 다시 눌러야 함 |
| DEV-07 | 팔레트 색상 배열 중복 정의 | ★★★ | 제안됨 | WORK_COLORS/GROUP_COLORS/MEMO_COLORS/TIMER_COLORS가 같은 7색을 파일마다 재정의 |

### 🎨 디자이너
| ID | 개선사항 | 중요도 | 상태 | 메모 |
|---|---|---|---|---|
| DES-02 | 홈 작품 카드 정보 과밀 | ★★★★ | 제안됨 | 진행률+칩 6개+최근작업이 한 카드에 몰림 (근본 원인) |
| DES-28 | 작품 카드 길이 세 번째 심화 | ★★★ | 제안됨 | 해시태그까지 추가되며 스크롤이 계속 길어짐 |
| DES-30 | 클라우드 "불러오기" 버튼에 파괴적 동작 강조 없음 | ★★★ | 제안됨 | 전체 데이터 교체인데 일반 버튼과 톤이 같음 |
| DES-11 | 홈 작품 카드 길이 심화 | ★★★ | 제안됨 | 빈 블록도 항상 렌더링돼 카드가 길어짐 |
| DES-20 | 신규 기능 발견 동선이 짧음 | ★★★ | 제안됨 | 온보딩이 최초 1회성이라 이후 추가 기능은 안내 안 됨 |
| DES-09 | 색상 커스터마이징 레이어 과다 | ★★★ | 제안됨 | 테마·작품·그룹·태그·메모·타이머 색이 다 따로 놀음 |
| DES-08 | 표 편집 버튼 상태 표시 부재 | ★★★ | 제안됨 | 커서가 표 안/밖이어도 버튼 모양이 항상 같음 |
| DES-21 | 클라우드 동기화 UI 발견성 낮음 | ★★ | 제안됨 | 설정 탭 안에 묻혀 있음 |
| DES-04 | 이모지 아이콘 혼용으로 톤 불일치 | ★★ | 제안됨 | OS/브라우저마다 렌더링 달라짐 |
| DES-06 | 빈 상태(empty state) 디자인 불일치 | ★★ | 제안됨 | 화면마다 아이콘형/텍스트형이 섞여있음 |

### 📊 PM(MD)
| ID | 개선사항 | 중요도 | 상태 | 메모 |
|---|---|---|---|---|
| MD-01 | 협업/공동집필 기능 부재 | ★★★★★ | 제안됨 | 최우선 항목 — MD-02 서버 저장소 기반으로 확장 가능 |
| MD-21 | 수동 클라우드 동기화의 "최신 상태 관리" 부담 | ★★★ | 제안됨 | 저장/불러오기 순서를 헷갈리면 최신 작업이 덮어써질 위험 |
| MD-11 | AI 맞춤법 검사 서비스 지속성 리스크 | ★★★ | 제안됨 | 네이버 비공식 프록시 — 막히면 조용히 규칙 기반만 남음 |
| MD-12 | 미션 기능에 알림 없어 리텐션 효과 제한적 | ★★★ | 제안됨 | 앱을 직접 열어야만 진행 상황 확인 가능 |
| MD-14 | 자료 수집 기능에 검색/태그 없음 | ★★★ | 제안됨 | 통합 검색(Ctrl+K)에도 아직 미연동 |
| MD-05 | 독자·베타리더 피드백 루프 부재 | ★★★ | 제안됨 | 조회수/반응을 앱 안에서 확인할 방법 없음 |
| MD-08 | 습작 데이터 활용한 리텐션 장치 부족 | ★★★ | 제안됨 | 스트릭 데이터는 쌓이는데 알림/리포트가 없음 |
| MD-09 | 커뮤니티/SNS 공유 기능 없음 | ★★★ | 제안됨 | 관계도·대시보드 카드를 이미지로 공유할 방법 없음 |
| MD-22 | 6자리 PIN 분실 시 복구 수단 없음 | ★★ | 제안됨 | 계정 개념이 없어 PIN 잊으면 데이터 접근 불가 |
| MD-29 | 서버 비용 발생하는데 수익화 모델 없음 | ★★ | 제안됨 | 클라우드 동기화가 첫 실제 서버 비용 발생 기능 |

---

## 👨‍💻 개발자 관점

### 📅 2026-08-06 (진행 중 — 4건, 전체 재감사 아님)

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 |
|---|---|---|---|---|---|
| DEV-34 | styles.css(1,420줄+) 탐색성 낮음 | 단일 대형 CSS 파일에 섹션 주석은 있었지만 파일 맨 위에서 전체 목록을 한눈에 볼 방법이 없었음 | 파일 분할은 캐스케이드 순서 리스크가 있어 보류하고, 대신 파일 상단에 전체 섹션 목록 + `grep -n` 점프 팁을 담은 TOC 주석 추가 | ★★ | ✅ 완료 |
| DEV-31 | UI 로직(뷰 레이어) 테스트 커버리지 확대 | DEV-01 완료 이후에도 `richEditor.js`/`timer.js`/`home.js`/`manuscript.js`(895줄) 등 뷰·UI 로직은 여전히 테스트가 없었음 | `timer.test.js`(8건: 오늘 집중시간 누적/롤오버, 위젯 스케일 클램핑)·`onboarding.test.js`(4건: 퍼널 카운터) 신규 추가로 61→77건. `manuscript.js`/`richEditor.js`/`home.js`는 DOM 상호작용이 커서 여전히 미커버 | ★★★ | 진행중 (부분 완료) |
| DEV-32 | localStorage 키 파편화가 이번 세션에 2개 더 늘어남 | 이미 `sw-timer-color`/`sw-theme-mode`/`sw-collapsed-chapters` 등으로 흩어져 있던 localStorage 키에, 이번 세션 신규 기능(타이머 집중시간, 온보딩 퍼널)이 `sw-focus-today`·`sw-onboarding-funnel`을 또 개별 키로 추가함 — 네임스페이스/버전 관리 전략 없이는 이 패턴이 기능이 늘 때마다 반복될 것 | 이번 건은 단순 카운터라 당장 위험은 낮음. 다음에 localStorage 키를 새로 만들 때는 개별 `sw-*` 키 대신 `sw-state` 단일 JSON 객체로 통합하는 리팩터를 우선 검토 | ★★ | 제안됨 |
| DEV-33 | z-index 값이 여전히 파일 곳곳에 하드코딩 | 이번 세션에 `.proofread-bar`에 `z-index: 6`을 추가하면서 기존 `.rich-toolbar`의 `z-index: 5`와의 관계를 주석으로만 설명 — 스케일 변수(`--z-*`) 없이 숫자를 계속 직접 비교해가며 끼워 넣는 중 | `--z-sticky-1`, `--z-sticky-2`, `--z-popover` 등 이름 있는 스케일을 `:root`에 정의하고 전체 교체 | ★★ | 제안됨 |

### 📅 2026-08-02 (진행 중 — 10건)

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 |
|---|---|---|---|---|---|
| DEV-21 | 클라우드 동기화 PIN에 무단 접근 방지 장치 없음 | `netlify/functions/sync.js`가 6자리 숫자(백만 개 경우의 수) PIN만으로 데이터를 읽고 덮어쓰게 하는데, 요청 빈도 제한이 전혀 없어 스크립트로 PIN을 순차 스캔하면 다른 사용자의 백업을 읽거나 덮어써(파괴) 버릴 수 있음. 코드 주석과 설정 화면 안내 문구로 "보안용이 아님"을 명시했지만 근본적인 방어는 아님 | IP 또는 PIN 단위 요청 횟수 제한(Netlify Blobs에 카운터 저장) 추가, 또는 최소한 실패 응답에 지수 백오프 유도 힌트 포함 | ★★★★ | 제안됨 |
| DEV-24 | 클라우드 동기화 페이로드 크기 실제 한도 미검증 | `sync.js`는 자체적으로 20MB까지 허용하도록 짰지만, Netlify Functions(AWS Lambda 기반)의 실제 요청/응답 크기 한도는 그보다 훨씬 작음 — **[2026-08-02, 실사용자 리포트로 확인]** 실제로 "클라우드 저장 실패 413"이 발생함. 우리 코드의 20MB 체크에 도달하기 전에 인프라 레벨(약 6MB)에서 먼저 거부된 것 | 배포된 환경에서 실제 대용량(수 MB) 백업으로 저장/불러오기 실측, 한도를 코드 상수가 아니라 실측값에 맞게 하향 조정 | ★★★ | ✅ 완료 (클라이언트에서 내보내기 JSON을 `CompressionStream('gzip')`으로 압축 후 base64로 감싸 전송/저장하도록 변경 — `js/app.js`의 `gzipToBase64`/`base64ToGunzipJson`, `netlify/functions/sync.js`는 압축된 바이트를 그대로 통과시키기만 함. 서버 측 한도도 실제 플랫폼 한도에 맞춰 5.5MB로 하향. 실측 결과 텍스트 위주 원고는 70~95% 압축, 반복성 낮은 첨부 이미지는 대략 77% 압축 확인) |
| DEV-25 | 클라우드 동기화 네트워크 실패 시 재시도 없음 | `App.cloudSave`/`cloudLoad`가 `fetch` 실패(일시적 네트워크 오류 등) 시 바로 에러 토스트만 띄우고 끝 — 사용자가 수동으로 버튼을 다시 눌러야 함, 자동 재시도가 없음 | 짧은 지수 백오프로 1~2회 자동 재시도 후에도 실패하면 에러 표시 | ★★★ | 제안됨 |
| DEV-26 | work.tags가 검색/내보내기 활용 경로에 미연동 | 이번 세션에 추가한 작품 해시태그(`work.tags`)가 입력·표시만 가능하고, 통합 검색(Ctrl+K, `Search.searchAll`)이나 백업 파일 안에서 태그로 필터링하는 기능에는 아직 연결되지 않음 | `Search.searchAll`에 작품 태그를 검색 대상으로 포함, 홈 화면에 태그 필터 칩 추가 검토 | ★★ | 제안됨 |
| DEV-27 | 일정 추가 모달이 goals.js/home.js에 여전히 부분 중복 | 두 파일의 일정 모달이 필드 구성이 달라(홈 쪽엔 작품 선택 select 추가) 완전한 공용 함수로 합치지 못함 — 이번 세션에 메모 필드를 추가할 때도 두 곳에 각각 반영해야 했음(DEV-17 팝오버 위치 계산 중복과 같은 결) | 공통 필드(제목/날짜/종일/시간/메모)를 렌더링하는 공용 헬퍼를 만들고, 작품 선택 select만 옵션으로 추가하는 방식으로 통합 | ★★ | 제안됨 |
| DEV-28 | 캐릭터 관계도가 데이터 변경마다 캔버스 전체 재마운트 | 관계 추가/삭제, 그룹 변경, 캐릭터 이름 변경 등이 있을 때마다 `refreshMap()`이 `mapHandle`을 destroy 후 다시 mount — 노드 위치는 DB에 저장돼 복원되지만, 매번 캔버스를 통째로 재생성하다 보니 상세 정보를 조금만 고쳐도 지도가 깜빡이는 느낌을 줄 수 있음 | 노드/엣지 데이터만 갱신하고 캔버스는 유지하는 부분 업데이트 API를 `Graph.mountRelationshipMap`에 추가하는 방안 검토 | ★★ | 제안됨 |
| DEV-29 | 더블클릭 내비게이션이 터치 기기에서 미검증 | 연결망 노드의 "1클릭=이동, 더블클릭=선택"으로 바꾼 로직이 마우스 `dblclick` 이벤트 기반인데, 터치 스크린(더블탭)에서도 동일하게 동작하는지 실기기 검증이 없음 — 애초에 관계망 캔버스가 "마우스 전용"이라고 안내되어 있어 우선순위는 낮음 | 태블릿 등에서 실측, 필요시 터치 이벤트 핸들러 별도 검토 | ★★ | 제안됨 |
| DEV-22 | 관계 해시태그 추가 후 인물관계도 범례 미갱신 | `characters.js`의 관계망 범례(`legend`)는 화면 진입 시 1회 조회한 `tags`를 클로저로 재사용 — 화면을 연 채로 설정에서 새 관계 해시태그를 추가해도 지도가 빈 상태에서 다시 그려지는 경우(캐릭터가 0명→1명 등)가 아니면 범례에 새 태그가 나타나지 않음 | 캐릭터 화면에도 다른 화면들처럼 태그 목록을 매 `refreshMap()` 호출 시 재조회하거나, 최소한 설정 모달을 닫을 때 이벤트로 알려주기 | ★★ | 제안됨 |
| DEV-23 | 캐릭터 3열 스플릿뷰의 좁은 화면 대응 | 캐릭터 화면을 목록/관계도 탭 대신 목록·상세·관계도 3열 스플릿뷰로 합쳤는데(`char-split`), 768px 미만에서는 1열로 쌓이도록 처리했지만 그 사이 태블릿·좁은 데스크톱 폭(예: 900~1200px)에서는 3열이 매우 좁게 압축될 수 있음 — 실측 미검증 | 중간 폭 구간에서 관계도를 접었다 펼 수 있는 토글 추가, 또는 그 구간에서만 2열(목록+상세, 관계도는 탭으로)로 자동 전환 | ★★ | 제안됨 |
| DEV-30 | 미션 스트릭과 집필 스트릭 로직이 중복 구현 | `Models.getMissionProgress`의 스트릭 분기에 "오늘 미달성 시 어제부터 카운트" 보정을 이번 세션에 추가하면서, `Models.getWritingStreak`와 사실상 동일한 로직이 두 곳에 따로 존재하게 됨 | 날짜 커서 계산 로직을 공용 헬퍼로 뽑아 두 함수가 함께 사용하도록 리팩터링 | ★ | 제안됨 |

### 📅 2026-08-01 (과거 기록 — 10건)

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 |
|---|---|---|---|---|---|
| DEV-14 | CI 파이프라인 부재 | 이번 세션에 vitest 테스트가 8개→61개로 늘었지만, 커밋/PR 시 자동으로 실행되는 CI가 없어 회귀가 로컬에서 테스트를 깜빡 잊으면 그대로 병합될 수 있음 | GitHub Actions로 push/PR 시 `npm test` 자동 실행하는 워크플로 추가 | ★★★★ | 제안됨 |
| DEV-11 | 비공식 서드파티 API 의존 리스크 | 맞춤법 검사(네이버 SpellerProxy)와 사전 검색(표준국어대사전)이 모두 공식 계약 없는 프록시(`netlify/functions/*`) 방식이라 상대 서버가 예고 없이 응답 형식을 바꾸거나 차단할 수 있음. 현재는 실패 시 토스트/상태 메시지로 조용히 성능 저하될 뿐, 감지·모니터링 장치가 없음 | 프록시 함수에 실패율 로깅 추가, 관리자용 헬스체크 엔드포인트나 상태 배지 검토 | ★★★ | 제안됨 |
| DEV-12 | 서비스워커 캐시 버전 수동 관리 | `sw.js`의 `CACHE_NAME`을 개발자가 배포마다 손으로 올려야 캐시가 갱신됨(이번 세션에 v4→v5로 직접 수정하며 스테일 캐시 버그를 고침) — 다음 배포에서 잊으면 동일한 "화면이 이상하게 깨짐" 버그가 재발할 수 있음 | 빌드 시점 파일 해시 기반 캐시 키 자동 생성(빌드 스텝 필요) 또는 최소한 배포 체크리스트에 캐시 버전 갱신을 명시 | ★★★ | 제안됨 |
| DEV-13 | IndexedDB 스키마 버전 이력 미문서화 | `DB_VERSION`이 이번 세션에 5→6으로 또 올라감(`missions`, `researchPosts` 스토어 추가)인데, 어떤 버전에 어떤 스토어/필드가 왜 추가됐는지 코드 밖에 별도 기록이 없어 히스토리 추적이 어려움 | `db.js` 상단에 버전별 변경 이력 주석 테이블 유지(버전, 날짜, 추가된 스토어/필드, 사유) | ★★ | 제안됨 |
| DEV-16 | 사전/맞춤법 조회 결과 캐싱 없음 | `Proofreader.lookupWord`/`checkOnline`이 매 호출마다 실제 네트워크 왕복을 하는데, 같은 세션에서 같은 단어를 여러 번 조회해도(예: 검토 중 같은 단어가 여러 번 등장) 캐시 없이 매번 프록시를 거침 | 세션 내 메모리 캐시(Map) 추가로 동일 조회어 재요청 방지 | ★★ | 제안됨 |
| DEV-17 | 팝오버/컨텍스트메뉴 위치 계산 로직 중복 | `timer-popover`, `proofread-popover`, `memo-conn-menu`, `context-menu` 등이 각자 `getBoundingClientRect()` 기반 화면 경계 클램핑 코드를 개별적으로 구현 — 로직은 거의 동일한데 공용 헬퍼가 없음 | `Utils.positionNearElement(el, anchorRect)` 같은 공용 함수로 통합 | ★★ | 제안됨 |
| DEV-18 | 홈 화면 초기 로딩이 작품 수에 비례해 느려짐 | `Views.home`이 작품마다 통계·목표·최근활동·관계망·메모 5종 쿼리를 병렬로 실행(`Promise.all`) — 작품이 수십 개로 늘면 초기 렌더가 눈에 띄게 느려질 수 있음. 페이지네이션/가상 스크롤/지연 로딩 없음 | 카드 20개 이상부터 "더 보기" 페이지네이션 또는 뷰포트에 보이는 카드만 데이터 로딩(intersection observer) | ★★ | 제안됨 |
| DEV-19 | 글래스모피즘 backdrop-filter 성능/호환성 미검증 | 사이드바·모달·카드·팝업 등 12곳 이상에 `backdrop-filter: blur()`를 적용했는데, 저사양 기기에서의 렌더링 성능이나 구형 브라우저(구형 Firefox 등 backdrop-filter 미지원)에서의 저하 그래픽 확인이 안 됨 | 저사양 기기 실측, `@supports not (backdrop-filter: blur(1px))` 폴백 스타일(불투명 배경) 추가 검토 | ★★ | 제안됨 |
| DEV-15 | proofread-mark 해제 시 서식 태그 분할 | `Proofreader.unwrapMark`이 `<b>`처럼 서식이 걸린 텍스트의 일부만 감쌌던 마크를 해제할 때, 원래 하나였던 `<b>` 요소가 두 개의 인접 `<b>`로 쪼개짐(테스트로 확인, 렌더링은 시각적으로 동일하지만 DOM 구조가 지저분해짐) | `contentEl.normalize()` 이후 인접한 동일 태그(같은 스타일)를 병합하는 후처리 추가 검토(우선순위 낮음 — 실사용 영향 없음) | ★ | 제안됨 |
| DEV-20 | 미션 진행률 계산이 매번 전체 writingLog를 순회 | `Models.getMissionProgress`가 미션마다 `getWritingLogForWork`로 작품의 전체 집필 기록을 불러와 순회함 — 미션 개수와 기록이 함께 늘면 목표/일정 화면 렌더가 느려질 수 있음(현재 규모에서는 체감 문제 없음) | 날짜 범위를 인덱스로 좁혀 조회하거나, 미션 렌더링 시 writingLog를 1회만 불러와 여러 미션이 공유하도록 리팩터링 | ★ | 제안됨 |

### 📅 2026-07-31 (과거 기록 — 10건)

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 |
|---|---|---|---|---|---|
| DEV-01 | 자동화 테스트 전무 | `Models`/`DB` 레이어에 단위테스트가 하나도 없어, 관계태그·그룹 삭제 시 교차 참조 정리 로직 등을 손으로만 검증해야 함. 기능이 늘수록 회귀 위험 급증 | Vitest 등 경량 러너 도입, IndexedDB는 `fake-indexeddb`로 목킹해 `models.js` 핵심 CRUD부터 우선 커버 | ★★★★★ | ✅ 완료 |
| DEV-02 | 레거시 데이터 마이그레이션 전략 미흡 | `DB_VERSION`이 2→4로 오르며 관계유형(`type`→`tagIds`), `characterGroups` 등 스키마가 여러 번 바뀌었는데, 실제 마이그레이션은 `Models.relationshipTagIds()`의 읽기 시점 흡수뿐이라 신구 포맷이 계속 공존 | `openDB()` 성공 콜백에서 1회성 정규화 패스 실행, 버전별 마이그레이션 함수를 테이블로 관리 | ★★★★ | ✅ 완료 |
| DEV-03 | 전역 스코프 기반 스크립트 구조의 확장성 한계 | 22개 `<script>` 태그가 로드 순서에 의존하는 전역 렉시컬 스코프를 공유 — 실제로 `characters.js`/`inbox.js`/`research.js`가 모두 전역 함수 `renderListView`를 선언해 캐릭터 목록 화면이 빈 화면으로 깨지는 실사용자 버그로 이어짐(함수명 고유화로 수정, 근본 구조는 미해결) | esbuild/Vite로 ES 모듈 번들링 전환(정적 배포 방식은 유지, 빌드 스텝만 추가) | ★★★★ | 제안됨 (실사용 버그로 위험도 상향) — **[2026-08-06]** 사용자에게 지금 실제로 착수할지 물었고, "전체 파일 구조/배포 방식이 바뀌는 큰 작업"이라 보류 결정. 우선순위가 더 높은 작업부터 처리하기로 함(제안 자체는 유효) |
| DEV-05 | IndexedDB 오류 처리 부재 | 거의 모든 뷰에서 `await DB.x()` 호출에 try/catch가 없어, 저장 실패(쿼터 초과 등) 시 사용자에게 아무 피드백 없이 조용히 실패 | `DB` 레이어에 공통 에러 래퍼 추가, 실패 시 `UI.toast(msg, 'error')` 표준 호출로 통일 | ★★★★ | 제안됨 |
| DEV-06 | base64 이미지 인라인 저장으로 인한 DB 비대화 | `richEditor.js`의 이미지 삽입이 압축/용량 제한 없이 원본을 그대로 base64로 저장 — 내보내기(JSON)와 버전 기록이 급격히 커질 수 있음. 작품 아바타·자료 수집 첨부파일도 같은 패턴 | 삽입 시 canvas로 리사이즈·재인코딩해 용량 상한 적용(예: 최대 1200px, JPEG 품질 80%) | ★★★★ | 제안됨 |
| DEV-08 | 데이터 가져오기 시 검증 없음 | `DB.importAll`이 외부 JSON을 검증 없이 그대로 `put` — 손상되었거나 형식이 다른 백업 파일이 앱 상태를 깨뜨릴 수 있음 | import 전 필수 필드/타입을 검사하는 검증 레이어 추가, 실패 시 항목 단위로 스킵 + 요약 보고 | ★★★★ | 제안됨 |
| DEV-04 | contenteditable + execCommand 의존 | `richEditor.js`가 표준에서 폐기(deprecated)된 `document.execCommand`에 전적으로 의존해 브라우저별 동작 차이 리스크가 있음 | 단기적으로 기능 고정 운영, 장기적으로 Range/Selection API 기반 자체 커맨드 레이어로 단계적 전환 검토 | ★★★ | 제안됨 |
| DEV-07 | 팔레트 색상 배열 중복 정의 | `WORK_COLORS`·`GROUP_COLORS`·`MEMO_COLORS`·`TIMER_COLORS`가 사실상 동일한 7색을 파일마다 재정의 | `utils.js` 또는 신규 `constants.js`에 단일 소스로 통합 후 각 파일에서 참조 | ★★★ | 제안됨 (중복 소스 추가로 상향) |
| DEV-09 | 스플릿뷰 자동저장 경쟁 상태(race condition) 가능성 | 같은 장면을 스플릿뷰의 두 판에 동시에 열면 독립된 두 `RichEditor`/디바운스가 서로의 저장을 덮어쓸 수 있음(현재 잠금·경고 없음) | 같은 sceneId가 이미 다른 판에 열려 있으면 경고 토스트를 띄우거나 두 번째 판을 읽기 전용으로 전환 | ★★★ | ✅ 완료 |
| DEV-10 | 접근성(키보드/스크린리더) 미고려 | 관계망·메모 캔버스 등 커스텀 드래그 UI가 마우스 전용이라 키보드만으로는 조작 불가, ARIA 라벨도 없음 | 각 캔버스 UI에 포커스 가능한 대체 목록 뷰 제공, 핵심 인터랙션에 ARIA 라벨/role 추가 | ★★★ | ✅ 완료 — **[2026-08-06]** 범위를 캔버스 밖 아이콘 전용 버튼 전체로 확장: `title`만 있고 `aria-label`이 없던 버튼(사이드바 nav/collapse, 리치에디터 툴바 ~20개, 더보기/삭제/보관 등 목록형 버튼) 전수 점검 후 추가. 색상 스와치(`<div>` 기반)는 여전히 키보드 포커스 불가 — 남은 과제 |

### 🔥 최우선 항목 (★★★★★, 전체 날짜 통틀어)
- **DEV-01** 자동화 테스트 전무 — ✅ 완료 (2026-07-31)

---

## 🎨 디자이너 관점

### 📅 2026-08-06 (진행 중 — 3건, 전체 재감사 아님)

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 |
|---|---|---|---|---|---|
| DES-31 | 맞춤법 검사 줄바꿈 버그 재발 + 팔레트 버튼 글씨 안 보임 (사용자 실사용 리포트) | 사용자가 직접 보고: (1) 맞춤법 검사 중 문단 경계뿐 아니라 리스트(`<li>`)/표 셀 경계를 넘는 매칭도 같은 방식으로 DOM을 훼손할 수 있었음, (2) 파스텔 테마에서 `.btn--pal-work`("+ 새 작품") 등 버튼이 흰 글씨 고정이라 밝은 배경에 묻혀 안 보임 | (1) `Proofreader.markIssues`에 문단·리스트·표 셀 경계를 함께 보는 `lineContainer` 가드 추가, 회귀 테스트 2건. (2) `Theme.onColorFor`로 배경색별 흑/백 대비를 계산해 `--palette-N-oncolor` 변수를 만들고 `.btn--pal-*`에 적용 | ★★★★ | ✅ 완료 |
| DES-32 | 집중모드 진입/이탈이 순간적으로 전환됨 | 사이드바·원고 트리가 `display:none`으로 즉시 사라져 "몰입 전환"이라기보다 화면이 뚝 끊기는 느낌 | `display:none` 대신 width/opacity 축소로 바꿔 기존 사이드바 collapse 트랜지션(`width 0.2s`)을 재사용, `.view--manuscript`/`.manuscript-tree`에도 트랜지션 추가 | ★★ | ✅ 완료 |
| DES-33 | 작품 카드 색상이 테마와 무관 | "새 작품 만들기"의 색상 스와치(`WORK_COLORS`)가 테마(비비드/파스텔/모노)와 상관없는 고정 무지개색이라, 테마를 바꿔도 기존/신규 작품 카드 색은 안 맞을 수 있음 | 스와치를 `Theme.currentPalette().colors`에서 가져오도록 변경 — 사용자가 선택한 "새 작품부터 적용" 방식으로, 기존 카드 색은 유지 | ★★ | ✅ 완료 (신규 작품만 적용, 기존 카드 소급 반영은 범위 밖으로 명시적 보류) |

### 📅 2026-08-02 (진행 중 — 10건)

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 |
|---|---|---|---|---|---|
| DES-28 | 홈 작품 카드 길이 세 번째 심화 (DES-02·DES-11 참고) | 이번 세션에 작품 해시태그(`work-card__tags`)가 카드에 또 추가되어, 이미 두 차례 지적된 "카드가 길어지는 문제"가 세 번째로 심화됨 | 이제는 임시방편 대신 근본적으로 카드를 "요약형(기본)/상세형(펼치기)"으로 나누는 작업을 우선순위에 올릴 시점 | ★★★ | 제안됨 |
| DES-30 | 클라우드 "불러오기" 버튼에 파괴적 동작 강조가 없음 | 클라우드에서 불러오기는 이 기기의 데이터를 전체 교체하는 파괴적 동작인데, 버튼 스타일이 일반 `btn--ghost`로 다른 버튼과 톤이 같음(클릭 시 확인 모달은 있지만 버튼 자체의 시각적 경고는 없음) | 저장 삭제 버튼처럼 danger 톤이나 경고 아이콘을 버튼에 추가 | ★★★ | 제안됨 |
| DES-21 | 클라우드 동기화 UI 발견성 낮음 | 새로 추가된 클라우드 동기화(6자리 PIN)가 설정 > 데이터 관리 탭 안에 다른 옵션들과 나란히 묻혀 있어, "여러 기기에서 쓰고 싶다"는 니즈가 있는 사용자가 발견하기 어려울 수 있음 | 온보딩 투어나 두 번째 기기에서 첫 작품 생성 시 "기존 기기 데이터 불러오기" 안내 배너 추가 | ★★ | 제안됨 |
| DES-25 | 캐릭터 3열 스플릿뷰에서 각 영역이 상대적으로 좁아짐 | 목록/상세/관계도가 한 화면에 다 들어가며 노트북 화면 등에서는 각 영역의 글자·카드 크기가 이전(2열, 탭 전환)보다 작게 느껴질 수 있음 | 실사용 반응 확인 후 필요시 열 너비 비율 조정 또는 사용자가 직접 드래그로 조절 가능한 리사이저 검토 | ★★ | 제안됨 |
| DES-26 | 웹소설 장르 드롭다운이 16개로 길어짐 | 장르가 6종→16종으로 늘며 `<select>` 목록이 한 화면에 다 안 보여 스크롤이 필요해짐 | 검색 가능한 콤보박스로 전환하거나, 장르를 계열별(로맨스 계열/판타지 계열/기타)로 그룹핑 | ★★ | 제안됨 |
| DES-27 | 달력 날짜 클릭 기능에 대한 화면 안내 부재 | 날짜 칸을 클릭하면 일정 추가 모달이 열리도록 바꿨지만, hover 시 커서가 pointer로 바뀌는 것 외에는 "여기를 클릭하면 일정을 추가할 수 있다"는 설명이 화면에 없어 발견이 우연에 의존함 | 달력 섹션 설명 문구에 "날짜를 클릭해 일정을 바로 추가할 수 있어요" 한 줄 추가 | ★★ | 제안됨 |
| DES-29 | 인물관계도 범례 오버레이가 캔버스를 가릴 수 있음 | 관계 해시태그(범례)가 캔버스 좌상단에 고정 오버레이로 표시되는데, 관계 유형이 많아지면(예: 8개 이상) 여러 줄로 늘어나며 그 아래 있는 노드를 가릴 수 있음 | 범례를 접었다 펼 수 있게 하거나 캔버스 바깥(툴바 영역)으로 이동 | ★★ | 제안됨 |
| DES-22 | 신규 해시태그 입력 UI가 기존 태그 UI와 시각 언어가 다름 | 작품 키워드 해시태그 입력(`.hashtag-input`, 알약형 칩+✕ 제거 버튼)이 기존 "관계 해시태그" 선택 UI(`.hashtag-chip`, 토글형 칩)와 이름은 비슷하지만 모양·조작 방식이 다름 | 두 컴포넌트를 하나의 공용 스타일 언어로 통일하거나, 서로 다른 목적임을 시각적으로 구분 | ★ | 제안됨 |
| DES-23 | 연결망 더블클릭 규칙 변경에 대한 안내 부재 | 노드 클릭 동작을 "1클릭=이동만, 더블클릭=선택/화면 이동"으로 바꿨는데, 기존 단일 클릭 습관에 익숙한 사용자에게는 "클릭해도 반응이 없다"고 느껴질 수 있음 | 힌트 텍스트는 이미 갱신함(`rel-hint`, `graph-hint`) — 처음 몇 번은 살짝 강조 애니메이션으로 변경 사실을 눈에 띄게 알리는 것도 검토 | ★ | 제안됨 |
| DES-24 | 클라우드 PIN 입력 필드가 "비밀번호"처럼 안 보임 | 평문 텍스트 입력이라 일반 텍스트 필드와 구분이 안 됨(다만 다른 기기에 옮겨 적어야 하니 완전히 가리는 `type="password"`는 오히려 불편할 수 있는 트레이드오프) | 자물쇠 아이콘 등 시각적 힌트만 추가해 "이건 접근 코드다"라는 느낌을 살짝 강화 | ★ | 제안됨 |

### 📅 2026-08-01 (과거 기록 — 10건)

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 |
|---|---|---|---|---|---|
| DES-11 | 홈 작품 카드 길이 심화 (DES-02 참고) | 🕸️연결망/📌메모/📝최근작업 블록이 데이터 없어도 항상 렌더링되어(빈 상태 문구 포함) 카드 세로 길이가 크게 늘어남 — 작품이 여러 개면 스크롤이 매우 길어짐 | 빈 블록은 아예 숨기거나 접힌 상태로 시작, 혹은 카드를 "요약형/상세형" 토글로 전환 가능하게 | ★★★ | 제안됨 |
| DES-20 | 새 기능들의 발견 동선이 짧음 | 자료 수집, 미션, 인라인 맞춤법 검사, 전체 일정 달력 등 신규 기능이 모두 "메뉴에 조용히 추가"된 상태 — DES-01 온보딩 투어가 최초 1회성이라 이후 추가된 기능은 안내받지 못함 | 온보딩을 "최초 1회"에서 "새 기능이 추가되면 해당 기능 첫 방문 시 짧은 스팟라이트 1개"로 확장 | ★★★ | 제안됨 |
| DES-12 | 애니메이션 다중 발생 시 일관성 미검토 | 카드 진입 애니메이션(`cardRise`) + 호버 리프트 + sticky 툴바 + 페이지 전환(`viewFadeIn`)이 겹치는 화면에서 과도하게 느껴질 수 있음, 실사용자 반응 확인 전 | 실사용 테스트 후 필요시 카드 재정렬 시 진입 애니메이션 생략 | ★★ | 제안됨 |
| DES-13 | 파스텔 대비 자동보정이 배경 변화 대응 미검증 | `Theme.adjustForContrast`가 `--bg-elevated` 기준으로 계산되는데, 글래스모피즘 반투명 배경(`--glass-bg`) 위에서는 실제 배경이 더 밝거나 어두워질 수 있어 보정값이 완벽히 맞지 않을 가능성 | 글래스 배경 기준으로도 대비를 재계산하거나, 최소 안전 마진을 더 크게 설정 | ★★ | 제안됨 |
| DES-14 | 맞춤법 검사 인라인 마크의 작은 글자 크기 가독성 미검증 | 물결/점선 밑줄이 "본문 글자 크기: 작게" 설정이나 고배율 화면에서도 잘 보이는지 기본 크기에서만 확인함 | 다양한 폰트 크기/배율 조합에서 실제 확인, 필요시 밑줄 두께를 상대 단위로 조정 | ★★ | 제안됨 |
| DES-16 | 자료 수집 보드형 뷰에 스크롤/줌 없음 | 메모보드(`MemoCanvas`)는 팬/줌이 되는데, 자료 수집 보드는 고정 좌표계라 카드가 많아지면 화면 밖으로 벗어난 카드를 찾기 어려움 | 메모보드와 동일한 뷰포트 팬/줌 인터랙션 이식 검토 | ★★ | 제안됨 |
| DES-17 | 미션 종류(스트릭/누적/체크리스트) 시각 구분이 텍스트 배지뿐 | 세 종류 모두 같은 회색조 카드에 배지 텍스트로만 구분되어 목록이 길어지면 한눈에 종류를 구별하기 어려움 | 종류별 아이콘(🔥 스트릭 / 📊 누적 / ✅ 체크리스트)이나 좌측 색상 바로 구분 | ★★ | 제안됨 |
| DES-18 | 테마 조합 경우의 수 증가로 육안 검수 비현실화 | 다크/라이트/시스템 × 비비드/파스텔/모노 × 팔레트 5종 = 45개 조합을 매번 손으로 확인할 수 없음 | Playwright 기반 비주얼 리그레션 스냅샷 테스트 도입 검토(DEV-14 CI 도입과 함께) | ★★ | 제안됨 |
| DES-15 | 전체 일정 달력 이벤트 칩 텍스트 잘림 | 좁은 날짜 칸 안에서 일정 제목이 길면 말줄임표로 잘리고, `title` 속성 툴팁 외에는 전체 제목을 확인할 방법이 없음 | 짧게 hover하면 나타나는 커스텀 툴팁(다른 팝오버와 통일된 스타일)으로 전체 제목 표시 | ★ | 제안됨 |
| DES-19 | 타이머 위젯 리사이즈 핸들이 매우 작음 | 12px 크기의 코너 핸들이라 마우스 정밀도가 낮거나 터치 환경에서는 잡기 어려움 | 히트 영역(클릭 인식 범위)을 시각적 크기보다 크게 설정(예: 24px 투명 영역) | ★ | 제안됨 |

### 📅 2026-07-31 (과거 기록 — 10건)

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 |
|---|---|---|---|---|---|
| DES-01 | 온보딩 부재 | `[[위키링크]]`, 관계 해시태그, 그룹, 스플릿뷰, 타이머 등 핵심 기능이 많은데 첫 사용자에게 안내하는 투어나 툴팁이 전혀 없어 기능 발견율이 낮을 것 | 최초 실행 시 3~4단계 스팟라이트 투어 또는 빈 상태(empty state)에 기능 힌트 문구 추가 | ★★★★ | ✅ 완료 |
| DES-02 | 홈 작품 카드 정보 과밀 | 카드 하나에 진행률 바 + 통계 칩 6개 + 최근 작업까지 한번에 노출되어 스캔하기보다 읽어야 하는 카드가 됨 | 기본은 제목/진행률/최근 작업만 보이게 하고 나머지는 "더보기" 확장이나 hover로 이동 | ★★★★ | 제안됨 (DES-11, DES-28에서 계속 심화) |
| DES-03 | 팔레트 색상 대비(WCAG) 미검증 | 테마 팔레트(비비드/파스텔/모노) 색상이 디자인 의도로만 선정되고 명도 대비 기준 검증이 없어, 일부 조합에서 텍스트 가독성이 떨어질 수 있음 | 팔레트 등록 시 WCAG AA 대비(4.5:1) 자동 체크 스크립트 추가, 미달 색상은 등록 단계에서 걸러내기 | ★★★★ | ✅ 완료 (`Theme.adjustForContrast`로 텍스트 색만 자동 보정) — **[2026-08-06]** 실제로는 `--palette-N-text`(자동보정 변수)로 안 옮겨가고 원본 `var(--palette-N)`을 그대로 텍스트 색으로 쓰던 곳이 nav 활성 상태·`.btn--ghost.btn--pal-*`·`.badge-pal-*`·캘린더 이벤트 칩 등 여러 곳에 남아있던 걸 발견해 전수 교체(DES-31 참고). "자동보정 변수가 있다"와 "실제로 다 그걸 쓴다"는 다른 문제였음 |
| DES-05 | 반응형/모바일 레이아웃 부재 | `--sidebar-w` 고정폭과 다단 그리드가 데스크톱을 전제로 설계되어 태블릿/모바일 폭에서 레이아웃이 깨짐 | 사이드바를 좁은 화면에서 자동 접힘 + 그리드를 브레이크포인트별로 1열 전환 | ★★★★ | ✅ 완료 (~768px까지) |
| DES-08 | 표 편집 버튼의 상태 표시 부재 | 리치 에디터의 "+행/+열/-행/-열" 버튼이 커서 위치와 무관하게 항상 같은 모양으로 노출되어, 표 밖에서 눌러야 토스트로만 안내받는 구조 | 커서가 표 안에 있을 때만 버튼을 활성 스타일로 바꾸거나, 표 밖에서는 아예 비활성 처리 | ★★★ | 제안됨 |
| DES-09 | 색상 커스터마이징 레이어 과다 | 테마 팔레트 · 작품 색상 · 캐릭터 그룹 색상 · 관계 해시태그 색상 · 메모 카드 색상 · 타이머 색상 · 메모 그룹 테두리/채우기색까지 색 설정이 계속 늘어나 정신 모델이 복잡해짐 | 색상 설정 화면에 "이 색은 어디에 쓰이나요" 미니 가이드 추가, 장기적으로 색상 시스템 통합 검토 | ★★★ | 제안됨 (계속 심화 중) |
| DES-04 | 이모지 아이콘 혼용으로 브랜드 톤 불일치 | 시스템 이모지를 아이콘처럼 사용해 OS/브라우저마다 렌더링이 달라지고 앱 고유의 톤앤매너가 약함 | 핵심 내비게이션만이라도 통일된 라인 아이콘 세트(SVG 스프라이트)로 교체 | ★★ | 제안됨 |
| DES-06 | 빈 상태(empty state) 디자인 불일치 | 화면마다 빈 상태가 아이콘+제목+설명인 곳과 텍스트 한 줄뿐인 곳이 섞여 있어 완성도가 들쭉날쭉해 보임 | 공용 `.empty-state` 컴포넌트를 표준화하고 모든 목록형 화면에 일괄 적용 | ★★ | 제안됨 |
| DES-07 | 저장/삭제 피드백이 약함 | 저장 완료가 `저장됨 · 방금` 같은 작은 텍스트 인디케이터뿐이라 사용자가 실제로 저장되었는지 신뢰하기 어려움 | 저장 성공 시 짧은 체크 아이콘 애니메이션 등 시각적 피드백 추가 | ★★ | ✅ 완료 **[2026-08-06]** (저장 쪽만) — `.save-indicator`에 상태별 점(입력 중: 펄스, 저장 중: 회전 스피너, 저장됨: 초록 solid dot) 추가. 삭제 피드백 강화는 범위 밖 |
| DES-10 | 화면 배율 기반 글자 크기 조절의 한계 | 설정의 "화면 크기"가 `zoom` 속성으로 레이아웃 전체를 확대해, 텍스트만 키우고 싶은 사용자에게는 카드/버튼까지 과하게 커짐 | 본문 폰트 크기만 별도로 조절하는 옵션을 추가하고 `zoom`은 "전체 화면 배율"로 명확히 구분 표기 | ★★ | ✅ 완료 |

### 🔥 최우선 항목 (★★★★★, 전체 날짜 통틀어)
- 없음 (현재 최고 중요도는 ★★★★)

---

## 📊 MD(콘텐츠 기획) 관점

### 📅 2026-08-06 (진행 중 — 2건, 전체 재감사 아님)

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 |
|---|---|---|---|---|---|
| MD-31 | 온보딩 이탈 지점을 알 방법이 없음 | "둘러보기" 4단계 중 사용자가 어디서 건너뛰거나 닫는지 데이터가 전혀 없어, 개편이 필요한 단계를 추측에 의존해야 함 | 외부 analytics는 개인정보/서비스 의존성 문제가 있어 제외하고, localStorage(`sw-onboarding-funnel`)에 단계별 도달 횟수 + 종료 사유(완료/건너뛰기/닫기)만 기록. `Onboarding.getFunnelStats()`로 조회 | ★★ | ✅ 완료 (로컬 카운터만, 집계/시각화 UI는 없음 — 콘솔 조회 전용) |
| MD-32 | 타이머 사용이 집필 통계와 분리되어 있음 | 뽀모도로/자유 타이머로 집중한 시간이 대시보드의 다른 "오늘" 지표(연속 집필일, 오늘 목표)와 나란히 보이지 않아, 집중 시간과 실제 집필량의 관계를 한눈에 볼 수 없었음 | 간단한 버전으로 우선 구현: 오늘 누적 집중 초를 localStorage에 저장(`sw-focus-today`)하고 작품 대시보드 stat-row에 "오늘 집중" 타일 추가. 미션/스트릭 시스템과의 정식 연동(예: "매일 N분 집중" 미션 종류)은 범위가 더 커서 보류 | ★★ | ✅ 완료 (표시만, 목표/미션 연동은 보류) |

### 📅 2026-08-02 (진행 중 — 10건)

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 |
|---|---|---|---|---|---|
| MD-21 | 수동 클라우드 동기화의 "최신 상태 관리" 부담이 사용자에게 있음 | 실시간 동기화가 아니라 수동 저장/불러오기라, 기기 A에서 방금 쓴 내용을 기기 B에서 보려면 사용자가 직접 "A에서 저장 → B에서 불러오기" 순서를 챙겨야 함. 순서를 헷갈리면 최신 작업이 오래된 클라우드본으로 덮어써질 위험(불러오기는 전체 교체 방식) | "마지막 동기화" 시각 표시는 이미 구현함 — 불러오기 직전 "클라우드본이 이 기기보다 오래됐을 수도 있어요" 같은 타임스탬프 비교 경고 검토 | ★★★ | 제안됨 |
| MD-22 | 6자리 PIN 분실 시 복구 수단 없음 | 계정/이메일 개념이 전혀 없어 PIN을 잊어버리면 클라우드에 저장해둔 데이터에 다시 접근할 방법이 없음 | 앱의 단순함을 유지하는 대신, 저장/불러오기 성공 시 "꼭 기억/메모해두라" 안내를 눈에 띄게 강화 | ★★ | 제안됨 |
| MD-23 | 장르가 다양해졌지만 작품당 장르는 여전히 1개만 선택 가능 | 장르를 6종→16종으로 늘렸지만(로맨스판타지, 회귀·빙의·환생 등), 실제 웹소설은 "현대판타지+회귀"처럼 복합 장르가 흔함. 같은 세션에 추가한 자유 키워드 해시태그와 성격이 겹쳐 "장르를 왜 하나만 고르지?"라고 느낄 수 있음 | 장기적으로 장르를 다중 선택으로 바꾸거나, 첫 장르는 "설정 노트 템플릿 적용용" 단일 유지 + 나머지는 해시태그로 보완하도록 안내 | ★★ | 제안됨 |
| MD-24 | 클라우드 동기화 신규 기능에 대한 안내 부재 | "여러 기기를 오가며 쓰는 작가"라는 새 사용자 시나리오가 이번에 열렸는데, 이를 알리는 문구가 온보딩/공지 어디에도 없음(DES-21과 연결) | 설정 화면 첫 진입 시 또는 새 기기에서 첫 작품 생성 시 1회성 안내 | ★★ | 제안됨 |
| MD-25 | 확장된 16개 장르가 실제 플랫폼 분류와 얼마나 일치하는지 미검증 | 문피아·카카오페이지 등 실제 연재 플랫폼의 장르 분류 체계를 직접 대조하지 않고 브레인스토밍으로 목록을 구성함 — 이름이나 분류가 실사용자 기대와 다를 수 있음 | 실제 플랫폼별 장르 목록과 대조해 이름/분류를 다듬는 검수 패스 진행 | ★★ | 제안됨 — **[2026-08-06]** 장르 템플릿(MD-23 연장선) 추가 여부를 물었으나 "우선순위 미결정"으로 보류. 다음에 논의할 때는 어떤 장르가 빠졌는지부터 구체적으로 짚고 시작할 것 |
| MD-27 | 3열 스플릿뷰로 데스크톱 대형 화면 전제가 강해짐 | 캐릭터 화면이 목록·상세·관계도 3열을 동시에 보여주는 방향으로 가면서, 이 앱이 암묵적으로 "노트북보다 넓은 데스크톱 모니터 사용자"를 더 전제하게 됨 | 실제 사용자층이 주로 어떤 기기로 집필하는지(노트북/데스크톱/태블릿) 재점검, 필요시 좁은 화면 전용 뷰 모드 설계 | ★★ | 제안됨 |
| MD-28 | UX 규칙 변경 누적에 대한 사용자 공지 체계 없음 | 연결망 더블클릭 규칙 변경(DES-23), 맞춤법 검사 UX 개편(MD-18) 등 기존 사용 습관과 충돌하는 변경이 쌓이는데, 이를 정리해 보여주는 "업데이트 노트" 화면이 없음 | 설정 또는 홈 화면에 최근 변경사항을 요약하는 섹션 신설(MD-16과 통합 검토) | ★★ | 제안됨 |
| MD-29 | 서버 비용이 발생하는 기능이 생겼는데 수익화 모델은 여전히 없음 (MD-07 참고) | PIN 기반 클라우드 동기화가 Netlify Blobs 사용량이라는 실제 서버 비용을 발생시키는 첫 기능인데, 사용자가 늘어날수록 무료로 감당해야 하는 비용 구조가 됨 | 클라우드 동기화 용량/빈도에 제한을 두거나, 장기적으로 프리미엄 티어 설계 시 이 기능을 포함해 검토 | ★★ | 제안됨 |
| MD-30 | 기록은 느는데 활용(회고/리포트)은 그대로인 패턴 반복 (MD-08 참고) | 일정 메모, 작품 해시태그 등으로 쌓이는 데이터가 계속 늘고 있지만, 이를 활용한 주간 요약·회고 리포트 같은 기능은 여전히 없음 | MD-08(리텐션 알림)과 함께 "이번 주 기록 요약" 같은 기능을 하나의 이니셔티브로 묶어 검토 | ★★ | 제안됨 |
| MD-26 | 해시태그가 발견(다른 작품 찾기) 기능으로 이어지지 않음 | 작품 키워드 해시태그가 개인 정리용으로만 쓰이고, 같은 키워드를 쓰는 다른 작가의 작품을 찾는 등 커뮤니티/발견 기능(MD-09)과는 연결되지 않음 — 현재 완전 로컬 구조상 자연스러운 한계 | 장기적으로 서버 저장소(이번에 생긴 Netlify Blobs)를 활용한 공개 태그 브라우징 기능 검토 | ★ | 제안됨 |

### 📅 2026-08-01 (과거 기록 — 10건)

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 |
|---|---|---|---|---|---|
| MD-11 | 비공식 AI 맞춤법 검사의 서비스 지속성 리스크 | MD-06을 완료 처리했지만 실제 구현이 네이버 비공식 프록시에 의존(DEV-11과 동일 리스크) — 언제든 막히면 AI 검사 기능이 조용히 규칙 기반만 남는 수준으로 후퇴함 | 공식 유료 API 전환을 장기 로드맵에 등록, 단기적으로는 실패 메시지에 더 친절한 안내 추가 | ★★★ | 제안됨 |
| MD-12 | 미션 기능에 알림이 없어 리텐션 효과 제한적 (MD-08 참고) | 스트릭/누적 미션을 만들어도 사용자가 앱을 직접 열어야만 진행 상황을 확인함 | 브라우저 알림 권한 요청 + 미션 마감 임박/스트릭 끊김 위험 시 알림 발송 | ★★★ | 제안됨 |
| MD-14 | 자료 수집 기능에 검색/태그 없음 | 자료 수집(리서치) 게시글이 늘어나면 찾기 어려움 — 통합 검색(Ctrl+K)에도 아직 연동되지 않음 | 자료 수집 게시글을 통합 검색 인덱스에 포함, 게시글 태그 필드 추가 | ★★★ | 제안됨 |
| MD-13 | 전체 일정 달력의 외부 캘린더 연동 부재 | 여러 작품의 일정을 모은 달력이 생겼지만, 구글 캘린더 등 외부 일정 앱과는 연동되지 않음 | ICS(iCalendar) 형식 내보내기 버튼 추가 | ★★ | 제안됨 |
| MD-15 | 작품별 프로필(필명)이 작품마다 중복 입력 | 한 작가가 여러 작품을 필명 하나로 연재하는 경우가 흔한데, 작품마다 필명을 각각 입력해야 함 | "작가 프로필"을 작품과 분리된 앱 전역 개념으로 승격 | ★★ | 제안됨 |
| MD-16 | 신규 기능 발견성/공지 부재 | 자료 수집·미션·인라인 맞춤법 검사·전체 일정 달력 등이 공지 없이 조용히 추가됨(DES-20과 연결) | 설정 또는 홈 화면에 "새로운 기능" 변경 로그 섹션 추가 | ★★ | 제안됨 |
| MD-17 | 글래스모피즘 리디자인의 성능 체감 미측정 | 장시간 집필 특성상 배터리/발열에 민감한 사용자가 많을 것으로 예상되는데, `backdrop-filter` 다용에 따른 실제 배터리 소모 체감 조사가 없음(DEV-19와 연결) | 저사양 노트북/배터리 모드에서 실사용 테스트, 필요시 "가벼운 모드" 옵션 제공 | ★★ | 제안됨 |
| MD-18 | 맞춤법 검사 UX 전면 개편에 대한 사용자 안내 부재 | 모달 목록형에서 본문 인라인 표시형으로 UX가 완전히 바뀌었는데, 기존 사용법에 익숙한 사용자에게 변경을 알리는 장치가 전혀 없음 | 첫 사용 시 "이 기능이 이렇게 바뀌었어요" 1회성 안내 툴팁 | ★★ | 제안됨 |
| MD-19 | 헤비 유저(다작품 보유)의 홈 화면 체감 미검증 | 홈 카드가 길어지고 블록이 늘면서, 작품을 10개 이상 관리하는 사용자의 스크롤 피로도가 어느 정도인지 실측이 없음(DES-11과 연결) | 실제 다작품 보유 베타 사용자 대상 사용성 테스트, 필요시 "간단히 보기" 뷰 모드 제공 | ★★ | 제안됨 |
| MD-20 | 국어 특화 기능 심화와 다국어 확장 전략의 긴장 (MD-10 참고) | 맞춤법 검사·표준국어대사전 연동처럼 한국어 전용 기능이 core 가치로 자리잡을수록, 향후 글로벌 진출 시 이 기능들을 어떻게 대체/확장할지에 대한 전략이 없음 | "한국 시장 심화" vs "글로벌 확장" 중 우선순위를 명확히 하고, 국어 특화 기능은 로케일 분기 가능하게 설계 | ★★ | 제안됨 |

### 📅 2026-07-31 (과거 기록 — 10건)

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 |
|---|---|---|---|---|---|
| MD-01 | 협업/공동집필 기능 부재 | 완전 로컬 단일 사용자 구조라 편집자·베타리더와 원고를 공유하거나 함께 작업할 방법이 없음. 실제 창작 워크플로우에서는 피드백 루프가 필수적 | "읽기 전용 공유 링크"부터 시작해 장기적으로 서버 동기화 기반 실시간 공동편집 검토. **[2026-08-02]** MD-02에서 Netlify Blobs 기반 서버 저장소가 처음 생겨, 이를 확장하면 "PIN을 공유해 읽기 전용으로 보기" 같은 최소 협업 기능의 기반으로 재사용 가능 | ★★★★★ | 제안됨 |
| MD-02 | 클라우드 백업/동기화 없음 | 데이터가 브라우저 로컬 IndexedDB에만 존재해, 브라우저 데이터 삭제·기기 변경 시 작품 전체가 유실될 위험이 있음 | Google Drive/OneDrive 연동 또는 자체 클라우드 백업부터 우선 제공 | ★★★★★ | ✅ 완료 (**[2026-08-02] 실제 클라우드 동기화로 업그레이드**: Netlify Blobs(`netlify/functions/sync.js`)에 6자리 PIN 키로 전체 데이터 수동 저장/불러오기. 계정 없이 PIN만 쓰는 방식이라 브루트포스 방어는 없음 — DEV-21 참고) |
| MD-03 | 플랫폼 업로드용 내보내기 형식 부재 | 내보내기가 JSON 백업 하나뿐이라, 실제 연재 플랫폼에 올리기 위한 txt/docx 변환을 사용자가 수동으로 해야 함 | 장면/챕터를 회차 단위 txt 또는 docx로 내보내는 기능 추가 | ★★★★ | ✅ 완료 (Markdown/PDF/DOCX 3종, DOCX는 의존성 없이 직접 구현) — **[2026-08-06 재확인]** 국내 일부 출판사·플랫폼이 여전히 요구하는 HWP(한글)는 미지원 상태. 포맷이 복잡하고 수요 확인이 먼저 필요해 지금은 백로그에만 남김 |
| MD-06 | AI 보조 기능 부재 | 문장 교정·전개 제안 등 AI 보조 기능이 표준이 되어가는데, 이 앱은 순수 수동 입력 도구에 머물러 있어 차별점이 약함 | 문장 다듬기 등 선택적 AI 보조 기능을 별도 설정으로 추가 검토 | ★★★ | ✅ 완료 (규칙 기반 + 네이버 비공식 AI 맞춤법 검사를 결합한 인라인 교정. "전개 제안/이름 생성"은 범위 밖) |
| MD-05 | 독자·베타리더 피드백 루프 부재 | 완결/조회수/반응 등 외부 플랫폼 데이터와 연동되지 않아, 작가가 자신의 작품 반응을 앱 안에서 확인할 방법이 없음 | 연재 플랫폼 URL 등록 후 회차별 조회수/댓글수를 수동 입력/스크랩하는 간단한 트래커 제공 | ★★★ | 제안됨 |
| MD-08 | 습작 습관 데이터를 활용한 리텐션 장치 부족 | 목표/스트릭 데이터는 쌓이고 있지만 이를 활용한 알림·리마인드·주간 리포트 같은 재방문 유도 장치가 없음 | 주간 집필 요약 알림과 스트릭 위험 경고 추가 | ★★★ | 제안됨 (미션 기능으로 데이터 기반은 강화됐으나 알림 부재는 그대로) |
| MD-09 | 커뮤니티/SNS 공유 기능 없음 | 관계도, 설정집, 진행률 카드 등 시각적으로 공유하기 좋은 결과물이 많은데 이미지로 내보내거나 SNS에 공유하는 기능이 없음 | 관계망/대시보드 카드를 PNG로 내보내는 "공유용 이미지 생성" 버튼 추가 | ★★★ | 제안됨 |
| MD-04 | 장르별 템플릿/기획 지원 부재 | 장르별 설정 노트 템플릿이나 클리셰·기획 체크리스트가 없어, 신규 작가가 빈 화면에서 막막함을 느낄 수 있음 | 작품 생성 시 장르 선택 → 해당 장르의 설정 노트 카테고리를 기본 시드로 제공 | ★★★ | ✅ 완료 (16개 장르 × 설정 노트 스텁 2~3개, 2026-08-02에 6→16종으로 확장) |
| MD-07 | 수익화 모델 부재 | 완전 무료 로컬 앱으로 수익 구조가 없어 지속적인 기능 개발·유지보수 재원을 확보하기 어려움 | 클라우드 동기화·협업·AI 보조 등 고급 기능을 프리미엄 티어로 설계 | ★★ | 제안됨 |
| MD-10 | 다국어/현지화 부재 | UI 전체가 한국어 전용이라, 글로벌 웹소설 시장으로 확장할 때 진입장벽이 됨 | 문자열을 `i18n` 리소스 파일로 분리해 다국어 확장 가능한 구조부터 선반영 | ★★ | 제안됨 |

### 🔥 최우선 항목 (★★★★★, 전체 날짜 통틀어)
- **MD-01** 협업/공동집필 기능 부재 — 제안됨 (MD-02의 서버 저장소를 기반으로 확장 가능)
- **MD-02** 클라우드 백업/동기화 없음 — ✅ 완료 (실제 Netlify Blobs 기반 PIN 동기화, DEV-21의 보안 한계 참고)

---

## 전체 요약
- 개발자 관점 34건 / 디자이너 관점 33건 / MD 관점 32건, 총 99건
- 완료: 20건 — 2026-07-31 등록분(DEV-01,02,09,10 / DES-01,03,05,10 / MD-04,06), 2026-08-01 등록분(MD-02,03), 2026-08-02 등록분(DEV-24), 2026-08-06 등록분(DEV-34, DES-31,32,33, MD-31,32) + 2026-08-06에 완료 처리된 과거 등록분(DES-07)
- ★★★★★ 최우선 항목: 3건 중 2건 완료 (DEV-01, MD-02). 남은 1건 MD-01(협업/공동집필)은 MD-02의 서버 저장소(Netlify Blobs)를 기반으로 확장할 여지가 생김
- **문서 구조 변경(2026-08-02)**: 하나로 계속 이어 붙이던 표를 날짜별 표로 재구성. 완료 항목은 등록된 날짜의 표에만 남고 최신 표로 다시 옮기지 않으므로, "지금 봐야 할 것"은 항상 가장 최근 날짜 표(중요도 내림차순 정렬)만 보면 됨
- **2026-08-06 세션 성격**: 전체 재감사가 아니라 사용자가 직접 지목한 11개 항목(번들러/테스트/CSS 정리/대비 감사/아이콘 라벨/집중모드/저장 인디케이터/내보내기 포맷/온보딩/타이머 통계 — 장르 우선순위는 보류)을 처리. 그래서 오늘 표는 각 관점 10건을 채우지 않고 실제로 다룬 항목만 등록했고, 기존 항목 중 겹치는 것은 새 ID를 만들지 않고 원래 항목에 날짜 붙여 갱신함
- 2026-08-02 세션 작업 — 6자리 PIN 클라우드 동기화, 달력 날짜 클릭 일정 추가, 웹소설 장르 6→16종, 작품 해시태그, 연결망 더블클릭 규칙, 캐릭터 3열 스플릿뷰 — 을 반영해 각 관점 오늘 표를 10건씩(기존 3건 + 신규 7건)으로 채움. 새 서버 의존성(Netlify Blobs)이 생겼으므로 DEV-21(PIN 무단접근 방지 부재)이 다음 세션 최우선 후보. 같은 날 DEV-24(페이로드 크기 한도)가 실제로 "클라우드 저장 실패 413"으로 재현·수정됨 — gzip 압축 도입
- 다음 세션에서 새 제안이 나오면 그날 날짜로 새 표를 만들고 `DEV-35`, `DES-34`, `MD-33`부터 이어서 채울 것
