# Storyweaver 개선사항 누적 관리 문서

이 문서는 개발자 / 디자이너 / MD(콘텐츠 기획) 세 관점에서 발견한 개선사항을 **세션마다 누적**해서 관리합니다.

## 관리 규칙
- 새 항목은 각 관점 표의 **맨 아래에 다음 번호로 추가**합니다. 기존 항목은 삭제하지 않습니다.
- 항목이 처리되면 **상태만 갱신**합니다 (`제안됨` → `진행중` → `완료`/`보류`). 완료되어도 행은 남겨 이력으로 유지합니다.
- 중요도는 ★☆☆☆☆(낮음) ~ ★★★★★(매우 높음) 5단계입니다.
- 표 아래 "🔥 최우선 항목"은 중요도 ★★★★★ 항목만 모은 요약이며, 항목 상태가 바뀔 때 함께 갱신합니다.

**최종 업데이트: 2026-08-01 (MD-06 완료 처리, DEV-11~20 · DES-11~20 · MD-11~20 신규 추가 — 맞춤법 검사·글래스모피즘·홈 대시보드·자료수집·미션 등 이번 세션 대규모 작업 반영)**

---

## 👨‍💻 개발자 관점

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 | 등록일 |
|---|---|---|---|---|---|---|
| DEV-01 | 자동화 테스트 전무 | `Models`/`DB` 레이어에 단위테스트가 하나도 없어, 이번 세션처럼 관계태그·그룹 삭제 시 교차 참조 정리 로직 등을 손으로만 검증해야 함. 기능이 늘수록 회귀 위험 급증 | Vitest 등 경량 러너 도입, IndexedDB는 `fake-indexeddb`로 목킹해 `models.js` 핵심 CRUD부터 우선 커버 | ★★★★★ | 완료 | 2026-07-31 |
| DEV-02 | 레거시 데이터 마이그레이션 전략 미흡 | `DB_VERSION`이 2→4로 오르며 관계유형(`type`→`tagIds`), `characterGroups` 등 스키마가 여러 번 바뀌었는데, 실제 마이그레이션은 `Models.relationshipTagIds()`의 읽기 시점 흡수뿐이라 신구 포맷이 계속 공존 | `openDB()` 성공 콜백에서 1회성 정규화 패스 실행, 버전별 마이그레이션 함수를 테이블로 관리 | ★★★★ | 완료 | 2026-07-31 |
| DEV-03 | 전역 스코프 기반 스크립트 구조의 확장성 한계 | 22개 `<script>` 태그가 로드 순서에 의존하는 전역 렉시컬 스코프(`GROUP_COLORS`, `PREF_*` 등)를 공유 — `index.html`의 스크립트 순서를 실수로 바꾸면 조용히 깨짐. **[실제 발생 사례, 2026-08-01]** `characters.js`/`inbox.js`/`research.js`가 모두 전역 함수 `renderListView`를 선언해 나중에 로드되는 파일이 앞선 파일들을 덮어써 캐릭터 목록 화면이 완전히 빈 화면으로 깨지는 실사용자 버그로 이어짐(원인 규명 후 함수명을 파일별로 고유화해 수정, 근본적인 전역 스코프 구조 자체는 여전히 미해결) | esbuild/Vite로 ES 모듈 번들링 전환(정적 배포 방식은 그대로 유지 가능, 빌드 스텝만 추가) | ★★★★ | 제안됨 (실사용 버그로 위험도 상향) | 2026-07-31 |
| DEV-04 | contenteditable + execCommand 의존 | `richEditor.js`가 표준에서 폐기(deprecated)된 `document.execCommand`에 전적으로 의존해 브라우저별 동작 차이 리스크가 있음 | 단기적으로 기능 고정 운영, 장기적으로 Range/Selection API 기반 자체 커맨드 레이어로 단계적 전환 검토 | ★★★ | 제안됨 | 2026-07-31 |
| DEV-05 | IndexedDB 오류 처리 부재 | 거의 모든 뷰에서 `await DB.x()` 호출에 try/catch가 없어, 저장 실패(쿼터 초과 등) 시 사용자에게 아무 피드백 없이 조용히 실패 | `DB` 레이어에 공통 에러 래퍼 추가, 실패 시 `UI.toast(msg, 'error')` 표준 호출로 통일 | ★★★★ | 제안됨 | 2026-07-31 |
| DEV-06 | base64 이미지 인라인 저장으로 인한 DB 비대화 | `richEditor.js`의 이미지 삽입이 압축/용량 제한 없이 원본을 그대로 base64로 `scene.content`에 삽입 — 내보내기(JSON)와 버전 기록이 급격히 커질 수 있음. 이번 세션에 추가된 작품 아바타(`work.avatarDataUrl`)·자료 수집 첨부파일(`researchPost.attachments`)도 같은 패턴이라 대상이 더 늘어남(첨부파일은 5MB 상한 경고만 있고 리사이즈/재인코딩은 없음) | 삽입 시 canvas로 리사이즈·재인코딩해 용량 상한 적용(예: 최대 1200px, JPEG 품질 80%) | ★★★★ | 제안됨 | 2026-07-31 |
| DEV-07 | 팔레트 색상 배열 중복 정의 | `WORK_COLORS`(home.js) · `GROUP_COLORS`(characters.js) · `MEMO_COLORS`(memoCanvas.js)가 사실상 동일한 7색을 파일마다 재정의. 이번 세션에 `TIMER_COLORS`(timer.js)가 하나 더 늘어남 | `utils.js` 또는 신규 `constants.js`에 단일 소스로 통합 후 각 파일에서 참조 | ★★★ | 제안됨 (중복 소스 1개 추가로 상향) | 2026-07-31 |
| DEV-08 | 데이터 가져오기 시 검증 없음 | `DB.importAll`이 외부 JSON을 검증 없이 그대로 `put` — 손상되었거나 형식이 다른 백업 파일이 앱 상태를 깨뜨릴 수 있음. `missions`/`researchPosts` 등 신규 스토어가 늘면서 검증 대상 스키마도 함께 늘어남 | import 전 필수 필드/타입을 검사하는 검증 레이어 추가, 실패 시 항목 단위로 스킵 + 요약 보고 | ★★★★ | 제안됨 | 2026-07-31 |
| DEV-09 | 스플릿뷰 자동저장 경쟁 상태(race condition) 가능성 | 같은 장면을 스플릿뷰의 두 판에 동시에 열면 독립된 두 `RichEditor`/디바운스가 서로의 저장을 덮어쓸 수 있음(현재 잠금·경고 없음) | 같은 sceneId가 이미 다른 판에 열려 있으면 경고 토스트를 띄우거나 두 번째 판을 읽기 전용으로 전환 | ★★★ | 완료 | 2026-07-31 |
| DEV-10 | 접근성(키보드/스크린리더) 미고려 | 관계망·메모 캔버스 등 커스텀 드래그 UI가 마우스 전용이라 키보드만으로는 조작 불가, ARIA 라벨도 없음 | 각 캔버스 UI에 포커스 가능한 대체 목록 뷰 제공, 핵심 인터랙션에 ARIA 라벨/role 추가 | ★★★ | 완료 | 2026-07-31 |
| DEV-11 | 비공식 서드파티 API 의존 리스크 | 맞춤법 검사(네이버 SpellerProxy)와 사전 검색(표준국어대사전)이 모두 공식 계약 없는 프록시(`netlify/functions/*`) 방식이라 상대 서버가 예고 없이 응답 형식을 바꾸거나 차단할 수 있음. 현재는 실패 시 토스트/상태 메시지로 조용히 성능 저하될 뿐, 감지·모니터링 장치가 없음 | 프록시 함수에 실패율 로깅 추가, 관리자용 헬스체크 엔드포인트나 상태 배지 검토 | ★★★ | 제안됨 | 2026-08-01 |
| DEV-12 | 서비스워커 캐시 버전 수동 관리 | `sw.js`의 `CACHE_NAME`을 개발자가 배포마다 손으로 올려야 캐시가 갱신됨(이번 세션에 v4→v5로 직접 수정하며 스테일 캐시 버그를 고침) — 다음 배포에서 잊으면 동일한 "화면이 이상하게 깨짐" 버그가 재발할 수 있음 | 빌드 시점 파일 해시 기반 캐시 키 자동 생성(빌드 스텝 필요) 또는 최소한 배포 체크리스트에 캐시 버전 갱신을 명시 | ★★★ | 제안됨 | 2026-08-01 |
| DEV-13 | IndexedDB 스키마 버전 이력 미문서화 | `DB_VERSION`이 이번 세션에 5→6으로 또 올라감(`missions`, `researchPosts` 스토어 추가)인데, 어떤 버전에 어떤 스토어/필드가 왜 추가됐는지 코드 밖에 별도 기록이 없어 히스토리 추적이 어려움 | `db.js` 상단에 버전별 변경 이력 주석 테이블 유지(버전, 날짜, 추가된 스토어/필드, 사유) | ★★ | 제안됨 | 2026-08-01 |
| DEV-14 | CI 파이프라인 부재 | 이번 세션에 vitest 테스트가 8개→61개로 늘었지만, 커밋/PR 시 자동으로 실행되는 CI가 없어 회귀가 로컬에서 테스트를 깜빡 잊으면 그대로 병합될 수 있음 | GitHub Actions로 push/PR 시 `npm test` 자동 실행하는 워크플로 추가 | ★★★★ | 제안됨 | 2026-08-01 |
| DEV-15 | proofread-mark 해제 시 서식 태그 분할 | `Proofreader.unwrapMark`이 `<b>`처럼 서식이 걸린 텍스트의 일부만 감쌌던 마크를 해제할 때, 원래 하나였던 `<b>` 요소가 두 개의 인접 `<b>`로 쪼개짐(테스트로 확인, 렌더링은 시각적으로 동일하지만 DOM 구조가 지저분해짐) | `contentEl.normalize()` 이후 인접한 동일 태그(같은 스타일)를 병합하는 후처리 추가 검토(우선순위 낮음 — 실사용 영향 없음) | ★ | 제안됨 | 2026-08-01 |
| DEV-16 | 사전/맞춤법 조회 결과 캐싱 없음 | `Proofreader.lookupWord`/`checkOnline`이 매 호출마다 실제 네트워크 왕복을 하는데, 같은 세션에서 같은 단어를 여러 번 조회해도(예: 검토 중 같은 단어가 여러 번 등장) 캐시 없이 매번 프록시를 거침 | 세션 내 메모리 캐시(Map) 추가로 동일 조회어 재요청 방지 | ★★ | 제안됨 | 2026-08-01 |
| DEV-17 | 팝오버/컨텍스트메뉴 위치 계산 로직 중복 | `timer-popover`, `proofread-popover`, `memo-conn-menu`, `context-menu`, `rel-edit-panel` 등이 각자 `getBoundingClientRect()` 기반 화면 경계 클램핑 코드를 개별적으로 구현 — 로직은 거의 동일한데 공용 헬퍼가 없음 | `Utils.positionNearElement(el, anchorRect)` 같은 공용 함수로 통합 | ★★ | 제안됨 | 2026-08-01 |
| DEV-18 | 홈 화면 초기 로딩이 작품 수에 비례해 느려짐 | `Views.home`이 작품마다 통계·목표·최근활동·관계망·메모 5종 쿼리를 병렬로 실행(`Promise.all`) — 작품이 수십 개로 늘면 초기 렌더가 눈에 띄게 느려질 수 있음. 페이지네이션/가상 스크롤/지연 로딩 없음 | 카드 20개 이상부터 "더 보기" 페이지네이션 또는 뷰포트에 보이는 카드만 데이터 로딩(intersection observer) | ★★ | 제안됨 | 2026-08-01 |
| DEV-19 | 글래스모피즘 backdrop-filter 성능/호환성 미검증 | 사이드바·모달·카드·팝업 등 12곳 이상에 `backdrop-filter: blur()`를 적용했는데, 저사양 기기에서의 렌더링 성능이나 구형 브라우저(구형 Firefox 등 backdrop-filter 미지원)에서의 저하 그래픽 확인이 안 됨 | 저사양 기기 실측, `@supports not (backdrop-filter: blur(1px))` 폴백 스타일(불투명 배경) 추가 검토 | ★★ | 제안됨 | 2026-08-01 |
| DEV-20 | 미션 진행률 계산이 매번 전체 writingLog를 순회 | `Models.getMissionProgress`가 미션마다 `getWritingLogForWork`로 작품의 전체 집필 기록을 불러와 순회함 — 미션 개수와 기록이 함께 늘면 목표/일정 화면 렌더가 느려질 수 있음(현재 규모에서는 체감 문제 없음) | 날짜 범위를 인덱스로 좁혀 조회하거나, 미션 렌더링 시 writingLog를 1회만 불러와 여러 미션이 공유하도록 리팩터링 | ★ | 제안됨 | 2026-08-01 |

### 🔥 최우선 항목 (★★★★★)
- **DEV-01** 자동화 테스트 전무 — ✅ 완료 (`package.json`, `tests/` 추가, 이번 세션에 61개로 확장)

---

## 🎨 디자이너 관점

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 | 등록일 |
|---|---|---|---|---|---|---|
| DES-01 | 온보딩 부재 | `[[위키링크]]`, 관계 해시태그, 그룹, 스플릿뷰, 타이머 등 핵심 기능이 많은데 첫 사용자에게 안내하는 투어나 툴팁이 전혀 없어 기능 발견율이 낮을 것 | 최초 실행 시 3~4단계 스팟라이트 투어 또는 빈 상태(empty state)에 기능 힌트 문구 추가 | ★★★★ | 완료 | 2026-07-31 |
| DES-02 | 홈 작품 카드 정보 과밀 | 카드 하나에 진행률 바 + 통계 칩 6개 + 최근 작업까지 한번에 노출되어(`home.js` `work-card--rich`) 스캔하기보다 읽어야 하는 카드가 됨. **이번 세션에 🕸️연결망/📌메모/📝최근작업 3개 블록이 추가로 붙어 카드가 더 길어짐** — 애초 지적된 정보 과밀 문제가 완화되지 않고 오히려 심화됨 | 기본은 제목/진행률/최근 작업만 보이게 하고 나머지(3개 블록 포함)는 "더보기" 확장이나 hover로 이동 | ★★★★ | 제안됨 (심화로 우선순위 유지/상향) | 2026-07-31 |
| DES-03 | 팔레트 색상 대비(WCAG) 미검증 | 테마 팔레트(비비드/파스텔/모노) 색상이 디자인 의도로만 선정되고 명도 대비 기준 검증이 없어, 일부 조합에서 텍스트 가독성이 떨어질 수 있음 | 팔레트 등록 시 WCAG AA 대비(4.5:1) 자동 체크 스크립트 추가, 미달 색상은 등록 단계에서 걸러내기 | ★★★★ | 완료 (`Theme.adjustForContrast`로 텍스트 색만 자동 보정하는 방식으로 구현, 팔레트 원색 자체는 그대로 유지) | 2026-07-31 |
| DES-04 | 이모지 아이콘 혼용으로 브랜드 톤 불일치 | 🔍⚙⏱🗺️ 등 시스템 이모지를 아이콘처럼 사용해 OS/브라우저마다 렌더링이 달라지고 앱 고유의 톤앤매너가 약함. 이번 세션에도 📎(자료수집)·🏆(미션)·✒️(필명) 등 이모지 아이콘이 계속 추가됨 | 핵심 내비게이션만이라도 통일된 라인 아이콘 세트(SVG 스프라이트)로 교체 | ★★ | 제안됨 | 2026-07-31 |
| DES-05 | 반응형/모바일 레이아웃 부재 | `--sidebar-w` 고정폭과 다단 그리드가 데스크톱을 전제로 설계되어 태블릿/모바일 폭에서 레이아웃이 깨짐 | 사이드바를 좁은 화면에서 자동 접힘(이미 있는 접기 기능과 연계) + 그리드를 브레이크포인트별로 1열 전환 | ★★★★ | 완료 (~768px까지; 손가락 조작 전제의 폰 폭 대응은 범위 밖) | 2026-07-31 |
| DES-06 | 빈 상태(empty state) 디자인 불일치 | 화면마다 빈 상태가 아이콘+제목+설명(`.empty-state`)인 곳과 텍스트 한 줄(`muted`)뿐인 곳이 섞여 있어 완성도가 들쭉날쭉해 보임. 이번 세션에 추가한 홈 카드 블록의 빈 상태(`work-card__block-empty`)도 텍스트 한 줄 방식이라 이 불일치에 새 사례를 하나 더함 | 공용 `.empty-state` 컴포넌트를 표준화하고 모든 목록형 화면에 일괄 적용(카드 내부처럼 좁은 공간엔 축소형 변형 정의) | ★★ | 제안됨 | 2026-07-31 |
| DES-07 | 저장/삭제 피드백이 약함 | 저장 완료가 `저장됨 · 방금` 같은 작은 텍스트 인디케이터뿐이라 사용자가 실제로 저장되었는지 신뢰하기 어려움. 이번 세션에 맞춤법 검사 로딩엔 스피너를 추가했지만(범위 한정), 일반 저장 인디케이터는 그대로임 | 저장 성공 시 짧은 체크 아이콘 애니메이션 등 시각적 피드백 추가 | ★★ | 제안됨 | 2026-07-31 |
| DES-08 | 표 편집 버튼의 상태 표시 부재 | 리치 에디터의 "+행/+열/-행/-열" 버튼이 커서 위치와 무관하게 항상 같은 모양으로 노출되어, 표 밖에서 눌러야 토스트로만 안내받는 구조(`richEditor.js`) | 커서가 표 안에 있을 때만 버튼을 활성 스타일로 바꾸거나, 표 밖에서는 아예 비활성(disabled) 처리 | ★★★ | 제안됨 | 2026-07-31 |
| DES-09 | 색상 커스터마이징 레이어 과다 | 테마 팔레트 · 작품 색상 · 캐릭터 그룹 색상 · 관계 해시태그 색상 · 메모 카드 색상까지 5개 층의 색 설정이 분산되어 있어 사용자의 정신 모델이 복잡해짐. **이번 세션에 타이머 색상, 메모 그룹 테두리색/채우기색 분리(2개 레이어)가 추가로 늘어남** | 색상 설정 화면(설정 창)에 "이 색은 어디에 쓰이나요" 미니 가이드 추가, 장기적으로 색상 시스템 통합 검토 | ★★★ | 제안됨 (레이어 3개 추가로 심화) | 2026-07-31 |
| DES-10 | 화면 배율 기반 글자 크기 조절의 한계 | 설정의 "화면 크기"가 `zoom` 속성으로 레이아웃 전체를 확대해, 텍스트만 키우고 싶은 사용자에게는 카드/버튼까지 과하게 커짐 | 본문 폰트 크기만 별도로 조절하는 옵션을 추가하고 `zoom`은 "전체 화면 배율"로 명확히 구분 표기 | ★★ | 완료 | 2026-07-31 |
| DES-11 | 홈 작품 카드 길이 심화 (DES-02 참고) | 🕸️연결망/📌메모/📝최근작업 블록이 데이터 없어도 항상 렌더링되어(빈 상태 문구 포함) 카드 세로 길이가 크게 늘어남 — 작품이 여러 개면 스크롤이 매우 길어짐 | 빈 블록은 아예 숨기거나 접힌 상태로 시작, 혹은 카드를 "요약형/상세형" 토글로 전환 가능하게 | ★★★ | 제안됨 | 2026-08-01 |
| DES-12 | 애니메이션 다중 발생 시 일관성 미검토 | 카드 진입 애니메이션(`cardRise`) + 호버 리프트 + sticky 툴바 + 페이지 전환(`viewFadeIn`)이 겹치는 화면(예: 정렬 변경 직후 스크롤)에서 과도하게 느껴질 수 있음, 실사용자 반응 확인 전 | 실사용 테스트 후 필요시 카드 재정렬 시 진입 애니메이션 생략(정렬 변경은 "새로 생긴 카드"가 아니므로) | ★★ | 제안됨 | 2026-08-01 |
| DES-13 | 파스텔 대비 자동보정이 배경 변화 대응 미검증 | `Theme.adjustForContrast`가 `--bg-elevated` 기준으로 계산되는데, 이번 세션에 추가된 글래스모피즘 반투명 배경(`--glass-bg`) 위에서는 실제 배경이 더 밝거나 어두워질 수 있어 보정값이 완벽히 맞지 않을 가능성 | 글래스 배경 기준으로도 대비를 재계산하거나, 최소 안전 마진을 더 크게 설정 | ★★ | 제안됨 | 2026-08-01 |
| DES-14 | 맞춤법 검사 인라인 마크의 작은 글자 크기 가독성 미검증 | 물결/점선 밑줄이 "본문 글자 크기: 작게" 설정이나 고배율 화면에서도 잘 보이는지 기본 크기에서만 확인함 | 다양한 폰트 크기/배율 조합에서 실제 확인, 필요시 밑줄 두께를 상대 단위로 조정 | ★★ | 제안됨 | 2026-08-01 |
| DES-15 | 전체 일정 달력 이벤트 칩 텍스트 잘림 | 좁은 날짜 칸 안에서 일정 제목이 길면 말줄임표로 잘리고, `title` 속성 툴팁 외에는 전체 제목을 확인할 방법이 없음 | 클릭 시 이동하는 현재 동작에 더해, 짧게 hover하면 나타나는 커스텀 툴팁(다른 팝오버와 통일된 스타일)으로 전체 제목 표시 | ★ | 제안됨 | 2026-08-01 |
| DES-16 | 자료 수집 보드형 뷰에 스크롤/줌 없음 | 메모보드(`MemoCanvas`)는 팬/줌이 되는데, 새로 추가된 자료 수집 보드는 고정 좌표계라 카드가 많아지면 화면 밖으로 벗어난 카드를 찾기 어려움 | 메모보드와 동일한 뷰포트 팬/줌 인터랙션 이식 검토 | ★★ | 제안됨 | 2026-08-01 |
| DES-17 | 미션 종류(스트릭/누적/체크리스트) 시각 구분이 텍스트 배지뿐 | 세 종류 모두 같은 회색조 카드에 배지 텍스트로만 구분되어 목록이 길어지면 한눈에 종류를 구별하기 어려움 | 종류별 아이콘(🔥 스트릭 / 📊 누적 / ✅ 체크리스트)이나 좌측 색상 바로 구분 | ★★ | 제안됨 | 2026-08-01 |
| DES-18 | 테마 조합 경우의 수 증가로 육안 검수 비현실화 | 다크/라이트/시스템 × 비비드/파스텔/모노 × 팔레트 5종 = 45개 조합을 매번 손으로 확인할 수 없음(이번 세션도 라이트/다크 대표 화면만 스크린샷 검증) | Playwright 기반 비주얼 리그레션 스냅샷 테스트 도입 검토(DEV-14 CI 도입과 함께) | ★★ | 제안됨 | 2026-08-01 |
| DES-19 | 타이머 위젯 리사이즈 핸들이 매우 작음 | 12px 크기의 코너 핸들이라 마우스 정밀도가 낮거나 터치 환경에서는 잡기 어려움 | 히트 영역(클릭 인식 범위)을 시각적 크기보다 크게 설정(예: 24px 투명 영역) | ★ | 제안됨 | 2026-08-01 |
| DES-20 | 새 기능들의 발견 동선이 짧음 | 자료 수집, 미션, 인라인 맞춤법 검사, 전체 일정 달력 등 이번 세션 신규 기능이 모두 "메뉴에 조용히 추가"된 상태 — DES-01 온보딩 투어가 최초 1회성이라 이후 추가된 기능은 안내받지 못함 | 온보딩을 "최초 1회"에서 "새 기능이 추가되면 해당 기능 첫 방문 시 짧은 스팟라이트 1개"로 확장 | ★★★ | 제안됨 | 2026-08-01 |

### 🔥 최우선 항목 (★★★★★)
- 없음 (현재 최고 중요도는 ★★★★)

---

## 📊 MD(콘텐츠 기획) 관점

| ID | 개선사항 | 사유 | 방안 | 중요도 | 상태 | 등록일 |
|---|---|---|---|---|---|---|
| MD-01 | 협업/공동집필 기능 부재 | 현재 완전 로컬 단일 사용자(브라우저 IndexedDB) 구조라 편집자·베타리더와 원고를 공유하거나 함께 작업할 방법이 전혀 없음. 실제 창작 워크플로우에서는 피드백 루프가 필수적 | 최소 기능으로 "읽기 전용 공유 링크"(JSON 스냅샷 내보내기 기반)부터 시작해, 장기적으로 서버 동기화 기반 실시간 공동편집 검토 | ★★★★★ | 제안됨 | 2026-07-31 |
| MD-02 | 클라우드 백업/동기화 없음 | 데이터가 브라우저 로컬 IndexedDB에만 존재해, 브라우저 데이터 삭제·기기 변경 시 작품 전체가 유실될 위험이 있음. 창작물 손실은 사용자 신뢰를 회복 불가능하게 깨뜨림 | Google Drive/OneDrive 연동 또는 자체 클라우드 백업(주기적 자동 내보내기)부터 우선 제공 | ★★★★★ | 완료 (경량화: 실제 클라우드 연동 대신 7일 주기 백업 리마인더 토스트로 대체 — 서버 없는 환경 한계) | 2026-08-01 |
| MD-03 | 플랫폼 업로드용 내보내기 형식 부재 | 내보내기가 JSON 백업 하나뿐이라, 문피아·카카오페이지·네이버시리즈 등 실제 연재 플랫폼에 올리기 위한 txt/docx 변환을 사용자가 수동으로 해야 함 — 실사용 워크플로우가 여기서 끊김 | 장면/챕터를 회차 단위 txt 또는 docx로 내보내는 기능 추가(제목·구분선 서식 포함) | ★★★★ | 완료 (Markdown/PDF/DOCX 3종. DOCX는 의존성 없이 직접 구현한 ZIP+OOXML 라이터. 이번 세션에 원고 화면에서도 바로 접근 가능하도록 "인쇄 모드"/"내보내기" 버튼 추가) | 2026-08-01 |
| MD-04 | 장르별 템플릿/기획 지원 부재 | 판타지·로맨스·무협 등 장르별 설정 노트 템플릿이나 클리셰·기획 체크리스트가 없어, 신규 작가가 빈 화면에서 막막함을 느낄 수 있음 | 작품 생성 시 장르 선택 → 해당 장르의 설정 노트 카테고리/캐릭터 원형을 기본 시드로 제공 | ★★★ | 완료 (6개 장르 × 설정 노트 스텁 2~3개) | 2026-08-01 |
| MD-05 | 독자·베타리더 피드백 루프 부재 | 완결/조회수/반응 등 외부 플랫폼 데이터와 연동되지 않아, 작가가 자신의 작품 반응을 앱 안에서 확인할 방법이 없음 | 연재 플랫폼 URL을 등록하면 회차별 조회수/댓글수를 수동 입력하거나 스크랩할 수 있는 간단한 트래커 제공 | ★★★ | 제안됨 | 2026-07-31 |
| MD-06 | AI 보조 기능 부재 | 최근 웹소설 저작 도구 트렌드상 문장 교정·전개 제안·이름 생성 등 AI 보조 기능이 표준이 되어가는데, 이 앱은 순수 수동 입력 도구에 머물러 있어 경쟁 도구 대비 차별점이 약함 | 문장 다듬기/다음 전개 제안 등 선택적(옵트인) AI 보조 기능을 별도 설정으로 추가 검토 | ★★★ | 완료 (문장 다듬기 영역: 국립국어원 어문규범 규칙 기반 + 네이버 비공식 AI 맞춤법 검사를 결합한 인라인 교정 기능 구현. "전개 제안/이름 생성" 등 창작 보조 AI는 범위 밖 — 필요시 별도 항목으로 재등록) | 2026-07-31 |
| MD-07 | 수익화 모델 부재 | 완전 무료 로컬 앱으로 수익 구조가 없어 지속적인 기능 개발·유지보수 재원을 확보하기 어려움 | 클라우드 동기화·협업·AI 보조 등 고급 기능을 프리미엄 티어로 설계해 지속가능한 개발 재원 마련 | ★★ | 제안됨 | 2026-07-31 |
| MD-08 | 습작 습관 데이터를 활용한 리텐션 장치 부족 | 목표/스트릭 데이터는 쌓이고 있지만(`Models.getWritingStreak` 등) 이를 활용한 알림·리마인드·주간 리포트 같은 재방문 유도 장치가 없음. 이번 세션에 추가된 미션(연속달성/누적/체크리스트) 기능도 진행률 "기록"은 되지만 알림은 없어 이 문제를 완전히 해결하지 못함 | 주간 집필 요약 알림(브라우저 알림 또는 홈 화면 배너)과 스트릭 위험 경고 추가 | ★★★ | 제안됨 (미션 기능으로 데이터 기반은 강화됐으나 알림 부재는 그대로) | 2026-07-31 |
| MD-09 | 커뮤니티/SNS 공유 기능 없음 | 관계도, 설정집, 진행률 카드 등 시각적으로 공유하기 좋은 결과물이 많은데 이미지로 내보내거나 SNS에 공유하는 기능이 없어 바이럴 유입 채널이 비어 있음. 이번 세션에 추가된 글래스모피즘 디자인으로 카드 비주얼이 더 좋아져 공유 가치가 더 커짐 | 관계망/대시보드 카드를 PNG로 내보내는 "공유용 이미지 생성" 버튼 추가 | ★★★ | 제안됨 | 2026-07-31 |
| MD-10 | 다국어/현지화 부재 | UI 전체가 한국어 전용이라, 왓패드 등 글로벌 웹소설 시장으로 확장할 때 진입장벽이 됨. 이번 세션에 추가된 맞춤법 검사·표준국어대사전 연동은 한국어 전용 기능이라 국내 특화가 더 깊어짐(글로벌화와는 반대 방향) | 문자열을 `i18n` 리소스 파일로 분리해 다국어 확장 가능한 구조부터 선반영. 국어 특화 기능(맞춤법 검사 등)은 로케일별 기능 분기로 설계 | ★★ | 제안됨 | 2026-07-31 |
| MD-11 | 비공식 AI 맞춤법 검사의 서비스 지속성 리스크 | MD-06을 완료 처리했지만 실제 구현이 네이버 비공식 프록시에 의존(DEV-11과 동일 리스크) — 언제든 막히면 AI 검사 기능이 조용히 규칙 기반만 남는 수준으로 후퇴함. 사용자에게는 "AI 검사 실패" 메시지만 보이고 왜 그런지, 언제 복구되는지 알 방법이 없음 | 공식 유료 API(예: 국내 클라우드 AI 사업자의 맞춤법 API) 전환을 장기 로드맵에 등록, 단기적으로는 실패 메시지에 더 친절한 안내 추가 | ★★★ | 제안됨 | 2026-08-01 |
| MD-12 | 미션 기능에 알림이 없어 리텐션 효과 제한적 (MD-08 참고) | 스트릭/누적 미션을 만들어도 사용자가 앱을 직접 열어야만 진행 상황을 확인함 — 웹소설 작가들이 매일 여러 앱을 오가는 특성상 알림 없이는 "잊혀지는 기능"이 되기 쉬움 | 브라우저 알림 권한 요청 + 미션 마감 임박/스트릭 끊김 위험 시 알림 발송 | ★★★ | 제안됨 | 2026-08-01 |
| MD-13 | 전체 일정 달력의 외부 캘린더 연동 부재 | 홈 화면에 여러 작품의 일정을 모은 달력이 새로 생겼지만, 구글 캘린더 등 작가가 실제로 쓰는 외부 일정 앱과는 연동되지 않아 "또 하나의 확인해야 할 곳"이 늘어난 셈 | ICS(iCalendar) 형식 내보내기 버튼 추가 — 구독 형태까지는 서버가 필요해 범위 밖이나, 1회성 내보내기는 클라이언트만으로 가능 | ★★ | 제안됨 | 2026-08-01 |
| MD-14 | 자료 수집 기능에 검색/태그 없음 | 새로 생긴 자료 수집(리서치) 게시글이 늘어나면 찾기 어려움 — 통합 검색(Ctrl+K)에도 아직 연동되지 않음 | 자료 수집 게시글을 통합 검색 인덱스에 포함, 게시글 태그 필드 추가 | ★★★ | 제안됨 | 2026-08-01 |
| MD-15 | 작품별 프로필(필명)이 작품마다 중복 입력 | 한 작가가 여러 작품을 필명 하나로 연재하는 경우가 흔한데, 지금은 작품마다 필명을 각각 입력해야 함 | "작가 프로필"을 작품과 분리된 앱 전역 개념으로 승격하고, 작품은 그중 하나를 선택하는 방식으로 전환 검토 | ★★ | 제안됨 | 2026-08-01 |
| MD-16 | 신규 기능 발견성/공지 부재 | 자료 수집·미션·인라인 맞춤법 검사·전체 일정 달력 등이 공지 없이 조용히 추가됨 — 기존 사용자는 직접 메뉴를 둘러보지 않으면 새 기능이 생긴 걸 모를 수 있음(DES-20과 연결) | 설정 또는 홈 화면에 "새로운 기능" 변경 로그 섹션 추가 | ★★ | 제안됨 | 2026-08-01 |
| MD-17 | 글래스모피즘 리디자인의 성능 체감 미측정 | 장시간 집필 작업 특성상 배터리/발열에 민감한 사용자가 많을 것으로 예상되는데, `backdrop-filter` 다용에 따른 실제 배터리 소모 체감 조사가 없음(DEV-19와 연결) | 저사양 노트북/배터리 모드에서 실사용 테스트, 필요시 "가벼운 모드"(블러 비활성화) 옵션 제공 | ★★ | 제안됨 | 2026-08-01 |
| MD-18 | 맞춤법 검사 UX 전면 개편에 대한 사용자 안내 부재 | 모달 목록형에서 본문 인라인 표시형으로 UX가 완전히 바뀌었는데, 기존 사용법에 익숙한 사용자에게 변경을 알리는 장치가 전혀 없음 | 첫 사용 시 "이 기능이 이렇게 바뀌었어요" 1회성 안내 툴팁 | ★★ | 제안됨 | 2026-08-01 |
| MD-19 | 헤비 유저(다작품 보유)의 홈 화면 체감 미검증 | 홈 카드가 길어지고 블록이 늘면서, 작품을 10개 이상 관리하는 사용자의 스크롤 피로도가 어느 정도인지 실측이 없음(DES-11과 연결) | 실제 다작품 보유 베타 사용자 대상 사용성 테스트, 필요시 "간단히 보기" 뷰 모드 제공 | ★★ | 제안됨 | 2026-08-01 |
| MD-20 | 국어 특화 기능 심화와 다국어 확장 전략의 긴장 (MD-10 참고) | 맞춤법 검사·표준국어대사전 연동처럼 한국어 전용 기능이 core 가치로 자리잡을수록, 향후 글로벌 진출 시 이 기능들을 다른 언어로 어떻게 대체/확장할지에 대한 전략이 없음 | 로드맵 상 "한국 시장 심화" vs "글로벌 확장" 중 우선순위를 명확히 하고, 국어 특화 기능은 로케일 분기 가능하게 설계 원칙 수립 | ★★ | 제안됨 | 2026-08-01 |

### 🔥 최우선 항목 (★★★★★)
- **MD-01** 협업/공동집필 기능 부재
- **MD-02** 클라우드 백업/동기화 없음 — ✅ 완료 (경량화 버전: 백업 리마인더)

---

## 전체 요약
- 개발자 관점 20건 / 디자이너 관점 20건 / MD 관점 20건, 총 60건
- 완료: 12건 (DEV-01, DEV-02, DEV-09, DEV-10, DES-01, DES-03, DES-05, DES-10, MD-02, MD-03, MD-04, MD-06) — 2026-08-01
- ★★★★★ 최우선 항목: 3건 중 2건 완료 (DEV-01 ✅, MD-02 ✅ 경량화 버전). 남은 1건 MD-01(협업/공동집필)은 서버 인프라가 필요해 이 환경에서는 범위 밖
- 이번 세션(2026-08-01) 대규모 작업 — 맞춤법 검사(규칙+AI, 인라인 리뷰 UX), 홈 대시보드 블록화 + 전체 일정 달력, 자료 수집, 미션, 작품 프로필, 메모보드 확장, 글래스모피즘/애니메이션 리디자인 — 을 반영해 각 관점 10건씩 신규 등록. 다수 항목이 "새 기능이 기존 지적사항을 심화시킴"(DES-02/09, DEV-06/07 등) 유형이라 우선순위 재검토 시 함께 볼 것
- 다음 세션에서 새 제안이 나오면 각 표 하단에 `DEV-21`, `DES-21`, `MD-21`부터 이어서 추가할 것
